<!-- generated by sync-agents — тонкий вход Claude. Канон навыков: tools/skills/; модель: .exocortex/model.md. Руками правится только блок projects-map. -->

# CLAUDE.md — вход Claude в Personal Cognitive OS

Этот файл читается Claude автоматически в каждой сессии. Полная модель зон и принципы — [.exocortex/model.md](.exocortex/model.md).

## Язык и голос

- Общение и записи — на языке владельца (по умолчанию русский).
- **Авторский голос неприкосновенен.** При обработке материала AI — редактор и ассистент, не сочинитель. Транскрипт/документ = сырьё, не готовый текст. Сохраняй лексикон, метафоры и конструкции «A, а не B». Приём проверки: «сказал бы я это так?» — если формулировка звучит как AI-полировка, откатись к авторской.

## Карта зон (кратко)

`concepts/` знание · `About-Me/` профиль · `operation/` sessions+daily · `sources/` вход (inbox/mail/calendar/team-sessions/ai-sessions) · `projects/` дела · `areas/` направления · `tools/` мастерская (канон навыков в `tools/skills/`) · `.exocortex/` системная зона.

## Принципы (минимум)

- **Один концепт = одно ядро смысла**; карточки в `concepts/`.
- **Ничего не канонизируется без явного вердикта владельца** (draft → канон только через авторское ревью).
- **Чистый Markdown, без обязательных плагинов**; всё работает после копирования на другую машину.
- **Канон навыков — `tools/skills/`**; `.claude/` — генерируемая проекция (перегенерация: `/sync-agents`), руками не правится.

## Навыки

Ядро: `/review-concepts` (материал → авторское ревью → концепт), `/init-me`, `/new-project`, `/start-session`, `/close-session`, `/list-concepts`, `/teach-me`, `/sync-agents`, `/update-exocortex`. Проектные навыки ведут диалог через выбор вариантов (AskUserQuestion), не свободным текстом.

## Карта проектов

> Ведётся навыками `new-project` / `start-session` / `close-session`. Правится только блок между маркерами ниже — не редактируй вручную.

<!-- projects-map:start -->

| Проект | Статус | Последняя сессия | Следующий шаг |
|--------|--------|------------------|---------------|
| _(пусто — создай первый проект командой `/new-project`)_ | | | |

<!-- projects-map:end -->
