<!-- generated by sync-agents — тонкий вход для не-Claude агентов (Codex и др.). Канон навыков: tools/skills/; модель: .exocortex/model.md. -->

# AGENTS.md — вход агента в Personal Cognitive OS

Это личный экзокортекс владельца: его знание в Markdown, ты — сменный движок. Полная модель — [.exocortex/model.md](.exocortex/model.md).

## Правила

- Язык — язык владельца (по умолчанию русский). **Авторский голос неприкосновенен**: ты редактор, не сочинитель.
- **Ничего не канонизируется без явного вердикта владельца.**
- Канон навыков — `tools/skills/*/SKILL.md` (agent-neutral): прочти SKILL.md нужного навыка и исполни его шаги.
- **Codex:** навыки доступны нативно из `.agents/skills/` (или вызовом `$<skill>`); MCP-серверы и project config — из `.codex/config.toml`. Всё это — проекция канона `tools/skills/` / `tools/mcp/`.
- Статус экзокортекса по запросу — навык `status` (печатает в чат; аналог CLI-statusline, работает в Codex/Cowork/Claude Code).
- Не редактируй генерируемые проекции (`.claude/`, `.codex/`, этот файл) — правки канона в `tools/skills/`, затем `/sync-agents`.

## Карта зон

`concepts/` знание · `discovery/` гипотезы и исследовательские вопросы · `About-Me/` профиль · `operation/` sessions+daily · `sources/` вход (5 каналов-дропзон) · `projects/` дела · `areas/` направления · `archive/` завершённое (сквозной срез) · `tools/` мастерская · `.exocortex/` системная зона (manifest, model, agents, projections).

## Ключевые навыки

`capture-hypothesis` (гипотеза → `discovery/` без канонизации) · `review-concepts` (материал → авторское ревью → карточка концепта) · `archive-item` (завершённое → `archive/`, путь и причина сохраняются) · `init-me` (профиль) · `new-project` / `session` (проекты и сессии: `/start-session` · `/checkpoint-session` · `/close-session`) · `list-concepts` · `sync-agents` (перегенерация проекций) · `update-exocortex` (обновление каркаса из seed + миграция раскладки).
