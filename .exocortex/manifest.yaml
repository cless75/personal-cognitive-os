type: personal
tier: base                # уровень лестницы: base | pro | team | master (DM-EXO-8)
name: personal-cognitive-os
title: "Personal Cognitive OS — твой репозиторий концепций"
owner: "{{learner}}"
owner_email: "{{learner_email}}"
seeded_from: exocortex-bok@707
created: "{{date}}"
schema_version: 2          # v2 = раскладка DM-EXO-7 v2 (7 зон + .exocortex/)
init_state: seed           # seed → agents → scaffold → profile → first-concept → complete

# Агенты, из которых работает владелец. Проекции (.claude/, AGENTS.md, .cursor/)
# генерирует навык sync-agents — руками их не правят.
agents:
  - claude-code
  - cowork
  - codex

surfaces:
  - obsidian
  - cowork
  - claude-code
  - codex

# Отчуждаемый seed: скачивается напрямую, растёт в личный репозиторий концепций.
# LLM — внешний сменный движок; когнитивный слой (этот репо) принадлежит тебе.
# Модель зон и принципы — .exocortex/model.md.
