# .exocortex/agents/ — реестр подключённых агентов

Один файл на агента: `<agent>.md` — как подключён, где его проекция, особенности.

Список активных агентов — поле `agents:` в `../manifest.yaml` (источник правды для `sync-agents`). Добавил агента в манифест → запусти `/sync-agents` → проекция сгенерируется, сюда добавь заметку при необходимости.
